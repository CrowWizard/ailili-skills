# product scripts

- `run_collection_pipeline.cjs` — plan / dispatch / status / summary
- `run_one_task.cjs` — 单槽位 textgen+imagegen（dispatch 后台调用）
- `build_textgen_params.cjs` / `build_imagegen_prompt.cjs` — 单张直出构参
